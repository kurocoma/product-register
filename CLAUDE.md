# product-register — Claude Code 向けルート設定

## Obsidian vault の参照ルール（ドメイン境界）
- この案件のドメインは **kuro（副業EC・クロコマース）**。
- vault（C:/Users/hppym/dev/second-brain）を参照するときは
  `21_kuro/` と `30_Atlas/` のみ。**`20_dev/`（本業）と `10_Daily/` は読まない・検索しない**。
- 本業側の情報が必要になったら、読む前にユーザーに確認する。

（webui 配下の恒久ルールは `webui/CLAUDE.md` → `webui/AGENTS.md` を参照）
